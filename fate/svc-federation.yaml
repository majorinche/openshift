apiVersion: v1
kind: Service
metadata:
  labels:
    app: federation-50001
  name: federation-50001
spec:
  clusterIP: 
  ports:
  - name: 9394-tcp
    port: 9394
    protocol: TCP
    targetPort: 9394
  selector:
    deploymentconfig: federation-50001
  type: ClusterIP
