apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  labels:
    app: redis-50002
  name: redis-50002
spec:
  replicas: 1
  selector:
    app: redis-50002
    deploymentconfig: redis-50002
  template:
    metadata:
      labels:
        app: redis-50002
        deploymentconfig: redis-50002
    spec:
      containers:
      - env:
        - name: REDIS_PASS
          value: fate_dev
        image: docker-repo.sonic.com:443/library/redis:5.0.2
        imagePullPolicy: IfNotPresent
        name: redis-50002
        ports:
        - containerPort: 6379
          protocol: TCP
        volumeMounts:
        - mountPath: /data
          name: redis-50002
      volumes:
      - name: redis-50002
        persistentVolumeClaim:
          claimName: redis-50002

