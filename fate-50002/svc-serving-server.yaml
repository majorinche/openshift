apiVersion: v1
kind: Service
metadata:
  labels:
    app: serving-server-50002
  name: serving-server-50002
spec:
  clusterIP: 
  ports:
  - name: 8001-tcp
    port: 8001
    protocol: TCP
    targetPort: 8001
  selector:
    deploymentconfig: serving-server-50002
  type: ClusterIP
